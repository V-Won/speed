//  XMLHttpRequest js 에서 가져오는 DOM 관련 이벤트는 onload에 넣기.
window.addEventListener('load', () => {
	setTimeout(() => {
		
	}, 200)
})

// gnbSwiperTab
const gnbSwiperTab = (item) => {
	setTimeout(() => {
		
	}, 500);
}